# Career gallery and header refinement

## Scope
- Replace the horizontally scrolling top navigation with a fixed two-row header; center the second row and keep both rows scrollbar-free.
- Keep only the SDU Sønderborg campus image in Career Moments, using a softly rounded masonry tile.
- Add an accessible Add Photo dialog for image URL, title/caption, and optional year or tag.
- Store added gallery photos in browser storage so they remain after refresh, while keeping the built-in campus photo unchanged.

## Verification
- Check desktop and mobile header layout, dialog validation and close behavior, photo insertion, persistence after refresh, rounded gallery edges, and absence of browser errors.
